{
  "// note": "Type-checking config for the hosted Worker only. The Worker is bundled by wrangler (esbuild); this file is for editor DX and `tsc --noEmit`. It is NOT part of the npm build (that is the root tsconfig, which builds src/ only).",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "WebWorker"],
    "types": [],
    "strict": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "noEmit": true
  },
  "include": ["*.ts"]
}
